# ShopSphere Frontend

React + Vite + Redux Toolkit + React Router v8 Declarative Mode + Tailwind CSS.

Authentication, cart and wishlist are server-backed. The browser does not persist JWTs or cart state in localStorage.
